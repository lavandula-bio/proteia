## Why

(One or two sentences on why this change is needed.)

## What changed

(Which files were touched and what each change does.)

## Verification

(What was actually run and the results, e.g. `uv run pytest`, `uv run ruff check .`, manual GUI check. For doc-only changes write "read through the full diff".)

## Related issues

(`Closes #N`; write "none" if not applicable.)
